# 个人网站使用手册 · Lang Ming Personal Website Guide

---

## 一、打开网站

找到这个文件夹，双击 `index.html` 用浏览器打开即可：

```
c:\Users\78713\Desktop\python\巨潮抓取代码\personal-website\
├── index.html    ← 双击这个打开网站
├── styles.css    ← 样式（颜色、字体、布局）
├── script.js     ← 交互逻辑
├── avatar.png    ← 头像图片
└── cv.pdf        ← 你的简历（需要你自己放进去，见下）
```

---

## 二、放入你的 CV（简历 PDF）

> [!IMPORTANT]
> **步骤：将你的 PDF 文件重命名为 `cv.pdf`，然后复制到 `personal-website` 文件夹里。**

具体操作：

1. 找到你本地的简历 PDF 文件
2. 右键 → 重命名 → 改为 `cv.pdf`
3. 将 `cv.pdf` 拖进 `personal-website` 文件夹
4. 刷新网页，点击"Download CV"或"下载 CV"按钮就能打开了

**以后要更新简历：** 只需用新版 PDF 覆盖 `cv.pdf` 即可，文件名不要变。

---

## 三、中英双语切换原理（方便你日后修改）

网页右上角有 **`中文 / En`** 两个按钮，点击切换语言。

### 在 HTML 文件里如何找到中英文内容？

打开 `index.html`，你会看到两种标记：

**方式 A：短文本（如标题、按钮文字）**

```html
<span class="lang-cn">中文内容</span>
<span class="lang-en">English content</span>
```

**方式 B：长段落（如简介正文）**

```html
<div class="lang-cn">
  <p>这里是中文段落……</p>
</div>
<div class="lang-en">
  <p>This is the English paragraph...</p>
</div>
```

> [!TIP]
> 修改时：找到对应的 `lang-cn` 或 `lang-en` 块，直接编辑里面的文字即可。中英文内容**相互独立**，改一个不影响另一个。

### 只有中文版有的内容（如"生活随笔"）

用 `class="cn-only"` 标记，英文模式下自动隐藏，无需额外操作。

---

## 四、修改各个模块内容

### 4.1 修改个人简介（About）

在 `index.html` 搜索 `<!-- ── BIO TEXT ──`，找到下面两个块：

```html
<div class="lang-cn">
  <p>这里是中文自我介绍……</p>   ← 改这里（中文）
</div>
<div class="lang-en">
  <p>This is the English bio...</p>   ← 改这里（英文）
</div>
```

### 4.2 修改教育背景

搜索 `教育背景` 或 `Education`，找到 `<ul class="info-list">` 里的列表项：

```html
<li>
  <span class="info-year">2026–</span>
  <span>
    <span class="lang-cn">哲学博士候选人，斯图加特大学 & 雅典大学</span>
    <span class="lang-en">PhD Candidate in Philosophy, ...</span>
  </span>
</li>
```

**增加一条教育经历：** 复制整个 `<li>…</li>` 块，修改年份和内容。

### 4.3 修改研究领域标签

搜索 `<div class="tags lang-cn">` 和 `<div class="tags lang-en">`，在里面增删 `<span class="tag">标签名</span>`。

好的，非常好。

### 4.4 修改"当前研究"描述

搜索 `research-description`，找到：

```html
<p class="research-description lang-cn">
  这里是中文研究描述……     ← 改这里
</p>
<p class="research-description lang-en">
  English research description...   ← 改这里
</p>
```

---

## 五、添加论文（论著选编 / Publications）

目前该模块显示"（即将更新）"。**添加第一篇论文时：**

在 `index.html` 搜索 `id="publications"`，找到：

```html
<p class="empty-placeholder lang-cn">（即将更新）</p>
<p class="empty-placeholder lang-en">(Coming soon)</p>
```

**将这两行替换为：**

```html
<div class="pub-list">

  <!-- 论文一 -->
  <div class="pub-item" data-aos>
    <div class="pub-year">2025</div>
    <div class="pub-content">
      <h4 class="pub-title lang-cn">论文中文标题</h4>
      <h4 class="pub-title lang-en">English Paper Title</h4>
      <p class="pub-meta lang-cn">《期刊名》，第X期，页XX–XX。</p>
      <p class="pub-meta lang-en"><em>Journal Name</em>, Vol.X, pp. XX–XX.</p>
      <div class="pub-links">
        <a href="论文链接或pdf文件名.pdf" class="pub-link" target="_blank">PDF ↗</a>
      </div>
    </div>
  </div>

  <!-- 继续复制上面的块来添加更多论文 -->

</div>
```

> [!TIP]
> 如果有 PDF 文件，将 PDF 放入 `personal-website` 文件夹，`href` 改为文件名，如 `href="paper2025.pdf"`。

---

## 六、添加学术报告（Presentations）

搜索 `id="presentations"`，找到占位符，替换方式与"添加论文"类似：

```html
<div class="pub-list">
  <div class="pub-item" data-aos>
    <div class="pub-year">2025</div>
    <div class="pub-content">
      <h4 class="pub-title lang-cn">报告题目（中文）</h4>
      <h4 class="pub-title lang-en">Talk Title (English)</h4>
      <p class="pub-meta lang-cn">会议名称，城市，年月</p>
      <p class="pub-meta lang-en">Conference Name, City, Month Year</p>
    </div>
  </div>
</div>
```

---

## 七、修改"生活随笔"（仅中文版可见）

搜索 `id="essays"`，找到 `<ul class="essays-list">`，里面每条内容是：

```html
<li class="essays-item">
  <span class="essays-num">01</span>
  <div class="essays-content">
    <p>这里写你想说的内容</p>
  </div>
</li>
```

**增加一条：** 复制整个 `<li>…</li>` 块，改 `01` → `03`，修改内容。

---

## 八、修改联系邮箱

搜索 `ml.1701635@gmail.com`（全文共出现 2 次），替换为你的新邮箱即可。

---

## 九、修改头像

将你的照片改名为 `avatar.png`（建议正方形，最小 400×400 像素），复制到 `personal-website` 文件夹覆盖原文件，刷新网页即可。

> [!NOTE]
> 支持 `.png`、`.jpg`、`.jpeg`、`.webp` 格式。如果用其他文件名（如 `photo.jpg`），需要同时在 `index.html` 里搜索 `src="avatar.png"` 改成 `src="photo.jpg"`。

---

## 十、上线发布（可选）

| 方式                   | 操作                                                                    | 费用 |
| ---------------------- | ----------------------------------------------------------------------- | ---- |
| **GitHub Pages** | 注册 GitHub → 新建仓库 → 上传文件夹 → Settings → Pages → 开启      | 免费 |
| **Netlify**      | 进入[netlify.com](https://netlify.com) → 拖拽 `personal-website` 文件夹 | 免费 |
| **Vercel**       | 进入[vercel.com](https://vercel.com) → Import → 上传                     | 免费 |

发布后你会得到一个公开链接（如 `https://yourname.netlify.app`），可分享给他人。

---

## 十一、常见问题

**Q：修改文字后网页没有变化？**
A：保存 HTML 文件后，在浏览器里按 `Ctrl + Shift + R`（强制刷新）。

**Q：想改网页配色？**A：打开 `styles.css`，找第 6–32 行的 `:root { }` 块，改变量值：

- `--accent: #c9a96e` → 金色主色调
- `--bg: #0e0e0f` → 背景色
- `--text: #e8e6e1` → 正文字色

**Q：网站可以在手机上用吗？**
A：可以，网站已适配手机屏幕（响应式设计）。

**Q：语言偏好会被记住吗？**
A：会。浏览器会记住上次选择的语言（中/英），下次打开自动恢复。
